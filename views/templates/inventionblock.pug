mixin inventionblock(title, name, body)
  .titlecard
    .expand
      h3.expansion +
    .spacer
    .invention
      h2.invention
        span.nono no 
        =title
    .infocard
      h2.name Without
      h2.name=name
      p.infoparagraph=body
